# Backend Environment Variables Example
# Copiez ce fichier en .env et remplissez avec vos vraies valeurs

# Port du serveur
PORT=5000

# Environnement (development, production)
NODE_ENV=development

# Base de données SQLite (aucune installation requise)
# Le fichier sera créé automatiquement dans le dossier prisma
DATABASE_URL="file:./dev.db"

# JWT Secret (utilisez une clé secrète complexe en production)
JWT_SECRET=your_very_secure_jwt_secret_key_change_in_production_123456789

# Stripe API Keys (clés de test)
# Obtenez vos clés sur https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here

# URL du Frontend (pour CORS et redirections)
FRONTEND_URL=http://localhost:3000

# Notes:
# - Pour la production, utilisez des clés Stripe en mode live (sk_live_...)
# - Générez un JWT_SECRET sécurisé : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# - Ne commitez JAMAIS ce fichier avec de vraies clés !
